class BonusDrink
   remainder_quotient(n){
    (n/3 + n%3)/3
    }
  def self.total_count_for(amount){
    sum = amount + amount/3,
    while(remainder_quotient(n)){
      sum + remainder_quotient(remainder_quotient(n))
    }
      total_count_for = sum
    }
  end
end
